; NEVER commit real EOS secrets/credentials.
; Copy required values into a local secrets config or deployment secret store.
; Then enable OnlineServicesEOS / OnlineServicesEOSGS in the .uproject.
;
; [OnlineServices]
; DefaultServices=Epic
;
; Register an EOS product in Epic Developer Portal before use.
